import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { UiGeocoderComponent } from './ui-geocoder.component';

describe('UiGeocoderComponent', () => {
  let component: UiGeocoderComponent;
  let fixture: ComponentFixture<UiGeocoderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ UiGeocoderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(UiGeocoderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
